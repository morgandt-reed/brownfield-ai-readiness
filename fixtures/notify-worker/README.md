# notify-worker

Fixture repository. Tests and a containerised build, no pipeline at all, and a
runtime the shipped support table has no entry for — reported as unknown rather
than assumed supported.
