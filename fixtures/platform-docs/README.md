# platform-docs

Fixture directory with no build manifest. It exists so that a scan demonstrates
what happens to a directory the scanner does not recognise: it appears in the
`skipped` list with a reason, and is never silently dropped from the estate.
