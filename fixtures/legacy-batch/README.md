# legacy-batch

Fixture repository. The case that matters most for this repository's argument:
a test command can be inferred (`mvn -B test` always exists) and there are no
tests at all. An inferable command is not an oracle.
