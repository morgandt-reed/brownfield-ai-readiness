# invoicing-api

Fixture repository. Same archetype family as orders-api but nothing is fixed:
unbounded dependency ranges, no lockfile, no tests, no pipeline, and a runtime
that is past its published end of support.
